 // if (graph.get(c).size >0) {
        //     for(let letter of graph.get(c)) {
        //         inDegree[getCharIndex(letter)]--
        //         if (inDegree[getCharIndex(letter)] === 0) {
        //             queue.push(letter)
        //         }
        //     }
        // }